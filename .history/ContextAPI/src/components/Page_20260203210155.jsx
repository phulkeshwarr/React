import Navbar from './Navbar';

const Page = () => {
    // Import your context here
    // const { data } = useContext(YourContext);

    return (
        <div className="page">
            
            <Navbar />
        </div>
    );
};

export default Page;